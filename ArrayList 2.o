import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Main{
    public static void main(String ar[]){
        ArrayList<ArrayList<Integer>> mainList=new ArrayList<>();
        ArrayList<Integer> list=new ArrayList<>();
        list.add(1); list.add(2);
        mainList.add(list);
        
        ArrayList<Integer> list2=new ArrayList<>();
        list2.add(13); list2.add(11); list2.add(2); list2.add(17); list2.add(3);
        mainList.add(list2);
        
        for(int i=0; i<mainList.size(); i++){
            ArrayList<Integer> currList=mainList.get(i);
            for(int j=0; j<currList.size(); j++){
                System.out.print(currList.get(j)+" ");
            }
            System.out.println();
        }
        System.out.println(mainList);
        
        
        System.out.println();
        System.out.println();
        
       
        
        
        ArrayList<ArrayList <Integer>> mainList1=new ArrayList<>();
        ArrayList<Integer> list3=new ArrayList<>();
        ArrayList<Integer> list4=new ArrayList<>();
        ArrayList<Integer> list5=new ArrayList<>();
        for(int i=1; i<=5; i++){
            list3.add(i*1);
            list4.add(i*2);
            list5.add(i*3);
        }
        mainList1.add(list3);
        mainList1.add(list4);
        mainList1.add(list5);
        System.out.println(mainList1);
        for(int i=0; i<mainList1.size(); i++){
            ArrayList<Integer> currList=mainList1.get(i);
            for(int j=0; j<currList.size(); j++){
                System.out.print(currList.get(j)+" ");
            }
            System.out.println();
        }
        mainList1.remove(2);
        list2.remove(2);
        list2.remove(1);
        mainList1.add(list2);
        // list2.removeAll(Arrays.asList(1, 2));
        //This statement removes element not by indices but by value.
        System.out.println(mainList1);
    }
}
